apiVersion: v1
kind: Pod
metadata:
  name: emptydir-demo
  namespace: finance
spec:
  containers:
  - name: app-container
    image: nginx:1.25-alpine
    volumeMounts:
    - name: cache-volume
      mountPath: /data
  volumes:
  - name: cache-volume
    emptyDir: {}
