apiVersion: v1
kind: Pod
metadata:
  name: first-pod-example
  labels:
    app: first-pod-example
spec:
  containers:
  - name: first-pod-example-container
    image: dgruploads/tomcat-image
    ports:
    - containerPort: 8080
